# Changelog

## 0.1.0 - 2026-04-24

- First public alpha release.
- Support `Boss直聘` and `实习僧`.
- Use `DrissionPage + CDP` browser takeover only.
- Add explicit `rehearsal` and `apply` run modes.
- Add per-platform browser ports: `Boss=9222`, `SXS=9223`.
- Add structured analysis-friendly JSON logs with `runs`, `records`, and `analytics`.
- Add `job-hunter-doctor` environment self-check command.
- Add `pyproject.toml`, `requirements.txt`, `.gitignore`, sample config, license, CI, and contribution docs.

